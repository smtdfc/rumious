use crate::router::metadata::RouteMetadata;

pub struct RouteGenerator {}

impl RouteGenerator {
    pub fn new() -> Self {
        Self {}
    }

    pub fn generate(paths: Vec<String>) {
        let routes: Vec<RouteMetadata> = vec![];
    }
}
